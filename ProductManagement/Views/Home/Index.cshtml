@model List<ProductManagement.Models.Product>
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Product Management</h1>
    <p class="lead">Manage your products easily</p>
</div>

<div class="row mb-3">
    <div class="col">
        <a asp-controller="Product" asp-action="Create" class="btn btn-primary">Add New Product</a>
    </div>
</div>

<div class="row">
    <div class="col">
        @if (Model.Any())
        {
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Name</th>
                        <th>Price</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var product in Model)
                    {
                        <tr>
                            <td>@product.Name</td>
                            <td>@product.Price.ToString("C")</td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="alert alert-info">
                <p class="mb-0">No products available. <a asp-controller="Product" asp-action="Create">Add your first product</a></p>
            </div>
        }
    </div>
</div>
